--extra-index-url https://download.pytorch.org/whl/nightly/cu130

# ── Core deep learning ────────────────────────────────────────────────────────
# Lưu ý: Vì CUDA 13.0 hiện chỉ có ở bản nightly, khi cài đặt bằng file này bạn cần thêm cờ --pre
# Ví dụ: pip install --pre -r requirements.txt
torch>=2.1.0
torchaudio>=2.1.0
torchvision

# ── PhoWhisper / Whisper encoder ──────────────────────────────────────────────
# vinai/PhoWhisper-base is loaded via HuggingFace transformers
transformers>=4.37.0
huggingface-hub>=0.20.0
accelerate>=0.26.0

# ── Audio I/O ─────────────────────────────────────────────────────────────────
soundfile>=0.12.1
librosa>=0.10.1

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.66.0
numpy>=1.24.0

# ── Optional G2P (uncomment if needed) ───────────────────────────────────────
g2p-en>=2.1.0
phonemizer>=3.2.1

# ── Dataset Downloading ──────────────────────────────────────────────────────
kagglehub
datasets
hf-transfer
